services:
  server:
    build: .
    ports:
      - "8080:8080"
    command: ["-addr=:8080", "-shard-capacity=1024", "-rules-file=/data/rules.json"]
    restart: unless-stopped
    volumes:
      - rules-data:/data

volumes:
  rules-data:
